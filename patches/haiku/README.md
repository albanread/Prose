# Haiku fork patches

These apply on top of Haiku **hrev60122**. We maintain our own fork; see `haiku_virtualized.md` §1.5 for why none of this goes upstream.

| Patch | What it does | Why |
|---|---|---|
| 0001 | The arm64 kernel keeps a RAM copy of all its debug output, starting with the tag `HAIKU-RAMLOG-V1` | Apple's Virtualization.framework gives the guest no UART. `tools/hvgpu`'s RAM console reads this buffer from guest memory, including panics and KDL output |
| 0002 | `virtio_gpu` refuses GPUs that don't offer `VIRTIO_GPU_F_EDID` | Under VZ, app_server would otherwise open VZ's own virtio-gpu first and hang. Our custom GPU offers EDID |
| 0003 | `virtio_pci`, modern-interface fixes: (a) the driver's accepted features are written to `driver_feature(_select)`, not the read-only `device_feature(_select)`; (b) transitional devices (IDs 0x1000–0x103f) that expose the modern capabilities use the modern interface; (c) capabilities are read by `cap_len`, not `length`; (d) notify offsets start as "unset" and notifies are range- and magic-checked; (e) 64-bit queue addresses are written as two 32-bit halves; (f) interrupts that find ISR 0 are counted | (a) Haiku never told a modern device which features it accepted. QEMU shrugs; VZ's block and GPU devices refuse to operate without `VERSION_1`, which is why virtio-blk failed under VZ. (b) The legacy interface uses I/O ports, and on arm64 the PCI layer reports host address 0 for every I/O BAR (`pci_ram_address()` only knows memory ranges), so all legacy devices aliased whichever sat at I/O address 0: initializing virtio-net reset the disk. That was QEMU's `pci-blk`/`pci-scsi` failure. (c) VZ's notify regions are tiny, so `notify_off_multiplier` was never read and `virtio_net` notified random kernel addresses. (d)–(f) are defences and diagnostics, plus the spec's §4.1.3.1 access rule |
| 0004 | The EFI loader numbers CPUs in MADT order, with the CPU it runs on as 0 | VZ's MADT reports GICC "CPU interface number" 0 for every CPU, which is valid for GICv3. Using it as the id made every CPU look like the boot CPU, so none were started and the kernel waited for them forever |
| 0005 | The loader's PSCI calls follow SMCCC: x0–x3 are in/out, x4–x17 are clobbered, and the status is returned | The asm declared inputs only, which is wrong for any PSCI implementation that writes more than x0. It also logs each `CPU_ON` result |
| 0006 | The loader logs "Entering the kernel on the boot CPU" | Shows in the RAM console where the loader hands over |
| 0007 | `virtio_block` logs every request that fails or times out (op, sector, length, vectors, wait result, elapsed, status byte) | Distinguishes "device never answered" (timeout, status 255) from "device rejected" (status 1/2) without a debugger |
| 0008 | The virtio bus manager logs the device's offered and the driver's accepted feature bits, in hex, for every device | The negotiation is where both VZ failures hid |
| 0009 | Shutdown and reboot through PSCI `SYSTEM_OFF`/`SYSTEM_RESET`; the loader passes the conduit (HVC/SMC) in `kernel_args` | `arch_cpu_shutdown()` was a stub, so "Shut down" did nothing and VMs could only be force-stopped |
| 0010 | The loader reads UEFI `GetTime()` before `ExitBootServices` and the kernel's RTC counts on from it | There is no RTC on arm64 Haiku, and none at all under VZ; the clock started at 1970 |
| 0011 | `acpi_gpio_events`: ACPI GPIO-signalled events on a PL061 (`ARMH0061`, `_AEI` → `_Exx`/`_Lxx`); `acpi_button` enabled for arm64; the private `acpi.h` gains the GPIO resource | This is how VZ signals the power button. Only the registers Linux's pl061 driver touches are written, and only the event pins' bits: VZ's PL061 model terminates the VM on unexpected writes (`AFSEL`, or `IC` bits of unconfigured pins) |
| 0012 | Per-interrupt trigger configuration for GICv3 (`GICD_ICFGR`), and `arch_int_configure_io_interrupt()` on arm64 | All SPIs were level-sensitive; edge sources that pulse their line, like QEMU's GED, were lost |
| 0013 | `acpi_gpio_events` also handles the ACPI Generic Event Device (`ACPI0013`, `_EVT(irq)`) | QEMU's `virt` signals the power button this way; `system_powerdown` now ends in a clean PSCI `SYSTEM_OFF` |
| 0014 | Virtio shared memory (`VIRTIO_PCI_CAP_SHARED_MEMORY_CFG`) in `virtio_pci` and the bus manager (`get_shared_memory()`), plus the `prose_display` kernel driver for the S2 device (`docs/s2-display-device.md`): maps the surface pool, sets the mode, posts event buffers, publishes `graphics/prose_display/0`; milestone A draws a test pattern from a kernel thread | The zero-copy display. Region 0 shows up as a 64-bit BAR (BAR 4 at 0x180000000 on VZ), so this also exercises the 64-bit BAR path |
| 0015 | `prose_display.accelerant` (app_server draws into the cloned surface pool), the optional accelerant hook `B_COMMIT_RECTANGLES` in `Accelerant.h`, `AccelerantHWInterface` calling it after `_CopyBackToFront()` and `_DrawCursor()`, and the driver ioctls behind it (`CLONE_POOL`, `SET_MODE`, `COMMIT`; retrace semaphore owned by the opener) | The desktop on the zero-copy display, paced by the host's vsync. User space may not acquire kernel-owned semaphores, hence the ownership hand-over at `open()` |
| 0016 | Live resize: `PRDS_WAIT_MODE_HINT` ioctl (the wait happens inside the driver, so the semaphore stays kernel-owned), `prose_display_agent` server (launched from `data/launch/system`, follows the host window with `BScreen::SetMode()`), `REDRAW` → full commit; the retrace semaphore is only handed to the first opener | The guest desktop follows the host window, like guest tools on other hypervisors |
| 0017 | Tear-free commits: the accelerant's `B_COMMIT_RECTANGLES` hook sends `PRDS_COMMIT` synchronously and returns when the host has copied the rects (the vsync-paced commit thread is gone); `_DrawCursor()` commits only the cursor's frame; a pool bandwidth check at accelerant init | app_server may not overwrite a rect the host is still reading; the 16 ms window of the asynchronous commit was where tearing came from |
| 0018 | app_server's back buffer in the surface pool: optional accelerant hook `B_GET_BACK_BUFFER_CONFIG` in `Accelerant.h`, app_server's `DeviceBackBuffer` replacing `MallocBuffer` when the hook answers, the driver reserving the region after the largest front buffer | Both buffers app_server works with become reachable by the host's GPU — the prerequisite for a bb → display blit kernel |
| 0019 | `virtio_input`: a device whose configuration carries the `PRIN1` marker is queried from a static table after the standard configuration instead of with select/subsel writes | Apple's Virtualization.framework never delivers configuration writes to custom virtio devices; this is how hvgpu's own keyboard and tablet describe themselves. QEMU's devices still use the standard protocol |
| 0020 | `prose_display_agent` switches once the host window has stopped moving (250 ms quiet, 1.5 s cap); `PRDS_WAIT_MODE_HINT` returns at once when the host's size differs from the caller's last applied size | A drag no longer causes a mode switch every 170 ms trailing the corner, and no hint can be lost |
| 0021 | `virtio_net`: header size follows `VIRTIO_F_VERSION_1` (12 bytes on modern devices), the driver asks for `VIRTIO_NET_F_CSUM` and checks the negotiation result, MTU stored as a frame size; the bus manager logs a refused `FEATURES_OK` and retries with everything offered, then the bare transport | Networking never worked under VZ (misframed packets; Apple's virtio-net refuses any feature set without CSUM). Also latent on QEMU since patch 0003 drives transitional devices as modern |
| 0022 | `virtio_sound`: a real hmulti_audio driver (stereo S16 48 kHz playback, buffers paced on a steady schedule anchored to the device's completions); device manager publishes `/dev/<class>` directories after boot (upstream TODO); `virtio_pci` interrupts request a reschedule; media stack + `prose_tone` in the image | Sound through VZ's and QEMU's virtio-snd. Without the device-manager fix `/dev/audio` never existed; without the pacing the node dropped buffers on hosts that complete in bursts |
| 0023 | `prose_midi`: the Prose MIDI virtio device (ID 62) as `/dev/midi/prose/0`, a raw MIDI byte stream `midi_server` turns into a port; `prose_midi_test`; device manager: `midi` path tables, no directory for a module's own name | The guest's MIDI played by the Mac's General MIDI synth and exposed to CoreMIDI. Module under `drivers/audio/hmulti` because a virtio child's driver is searched only at registration, in the `/dev/audio/hmulti` context |
| 0024 | HostFS: `virtio_fs` (boot module) moves FUSE requests for virtio-fs devices and publishes each as a read-only pseudo-disk `/dev/disk/virtual/virtio_fs/<n>/raw` naming the device's tag; the `hostfs` file system (FUSE 7.31 client) recognises it and mounts it; node table with FORGET, file cache with close-to-open consistency; mount_server always mounts HostFS volumes | A folder on the Mac as a disk in Haiku (`hvgpu --share DIR`): mounted at boot at `/<tag>` (default `/HostFS`), on the Desktop and in Tracker's Mount menu. VZ's own virtio-fs device and FUSE server do the host side |
| 0025 | HostFS: Haiku attributes as host xattrs `user.haiku.<name>` (type code + data, the build tools' convention), `BEOS:TYPE` emulated from the extension when none is stored; directories read with `FUSE_READDIRPLUS` so `d_ino` is the vnode ID; idle nodes (≤ 2048) kept instead of forgotten at once | Tracker needs attributes for icons and types and starts node monitoring from `d_ino`; applications copied to the Mac keep their icons and signatures |
| 0026 | HostFS: attribute reads in one round trip (4 KiB first, size query only on `ERANGE`); `create_attr` without `O_TRUNC` keeps the data on a type change; `hostfs` also linked from `kernel/boot` | Tracker reads several attributes per file; the loader preloads every file system, and the preloaded image now keeps a valid path after boot |
| 0027 | HostFS review fixes: stale files are only marked from lookups/readdir and refreshed from hooks that hold the vnode (no panic when a mapped program changes on the Mac; its old pages stay until it exits); per-node size lock (write-back shared, size changes exclusive) and refresh gate, so concurrent appends and truncation cannot lose or resurrect data; close re-stamps only for the last writer; case-only renames reach the host; `virtio_fs` polls a queue whose completion is late and copies non-kernel addresses with `user_memcpy` | Found by an independent review and reproduced by `private_workspace/hostfs-hard.sh` (one lost append in 800 before the fix) |
| 0028 | `virtio_input`: Control+Command+Delete opens the Team Monitor, checked on the key state like the keyboard add-on (Delete down while the keymap's left Command and Control keys are down); the key events that follow go to the window until the first key-up, which stops the forced-reboot count and leaves the window open. `TeamMonitorWindow.cpp`/`TeamListItem.cpp` are built from the keyboard add-on's directory, and its `Keyboard.rdef` signature finds the Team Monitor's translations | The Team Monitor was only in the keyboard (PS/2, USB) add-on, so under VZ, whose keyboard is hvgpu's virtio-input device, ⌃⌘⌦ and Machine ▸ Send Keys ▸ Control-Alt-Delete did nothing |
| 0029 | arm64 `do_sync_handler()`: every exception from user mode becomes a real signal. Undefined instructions (EC 0x00: UDF, SVE on Apple's cores), SVE/SME access traps and illegal execution state → SIGILL `ILL_ILLOPC`, "Invalid opcode exception" in the debugger (PSTATE.IL cleared first); trapped system register/instruction access (EC 0x18: ID registers, DAIF) → SIGILL `ILL_PRVOPC`; SP alignment → SIGBUS `BUS_ADRALN`; trapped FP exceptions → SIGFPE; unresolved aborts → SIGBUS (alignment: `BUS_ADRALN`, external: `BUS_ADRERR` + syslog line); BRK → the debugger sees a breakpoint unless a SIGTRAP handler is installed, then SIGTRAP `TRAP_BRKPT`; any other class → SIGILL `ILL_ILLTRP` + syslog line; kernel mode panics with the ESR | The switch had no case for EC 0x00 and no default, so the code after it used uninitialised variables: an undefined instruction gave a garbage exception ("Alignment exception") and signal (SIGBUS at address 0), and programs that probe CPU features by catching SIGILL (OpenSSL, `codec_check`) hung in the debugger. `private_workspace/trap-test.sh [qemu\|vz]` checks each case |
| 0030 | virtio bus manager: after turning a queue's interrupt back on, a full barrier and another round of the driver's callback if the device's used index moved since the callback started (≤ 64 rounds); a rate-limited log of rounds that caught a completion (`virtio: device type T queue Q: N time(s) a completion came while its interrupt was off`) | A completion that landed between the callback's drain and the re-enable raised no interrupt and waited for the device's next one (the virtio spec requires the re-check). Caught 64+ times on virtio-fs under 8 parallel HostFS threads in one run; no regressions on blk, net, input, MIDI, S2, virtio-fs (VZ) and snd, blk, net (QEMU) |
| 0031 | PROSE artwork in `data/artwork` (logos, manuscript wallpapers; the "tiny" logos scaled to 158x51 for AboutSystem), shipped beside the stock artwork; Tracker's default desktop background is the PROSE wallpaper, scaled to fit | The branding. `uname` still says "Haiku": configure scripts and package builds detect the OS by that string |
| 0032 | `build/jam/packages/Haiku` adds `SYSTEM_ADD_ONS_TRANSLATORS` to the package | Every image profile listed translators, but none ever reached the image, so the desktop background could not be decoded |
| 0033 | The build takes third-party packages from `generated/download/` (`HAIKU_NO_DOWNLOADS=1` in the untracked BuildConfig); libpng16 listed in `repositories/HaikuPorts/arm64` and installed through `UserBuildConfig` | The HaikuPorts package list is checksummed against a server snapshot, so a locally built package cannot be added to it. Needs the packages and settings in `private_workspace/README.md` ("Local packages") |
| 0034 | `prose_display`: the colour-bar test card is off by default (`PRDS_TEST_PATTERN 1` restores it) | Milestone A scaffolding; it interrupted the host's static until the guest's first commit |
| 0035 | Wallpapers rendered by `tools/artwork/artwork.swift` (ink dark, the default; paper light) replace the manuscript ones; AboutSystem opens by saying this is an unofficial, experimental port of Haiku to Apple Silicon, not affiliated with the Haiku project, whose developers did the work | The ruled-paper wallpapers fought icon labels, and the About window should say plainly what this build is. 9 MB: the PNGs travel in the patch |
| 0036 | Deskbar: the PROSE mark, an HVIF icon written by `tools/artwork/hvif.py`, replaces the Haiku leaf | The wallpaper's broad-nib stroke; one bold stroke survives the Deskbar's 63x22 rendering |
| 0037 | Image translators: BMP, GIF, HVIF, ICO, PCX, PPM, PSD, RTF, SGI, STXT, TGA, and JPEG, PNG, TIFF, WebP from local packages (libjpeg_turbo, libpng16, tiff, libwebp, giflib) | The image shipped no translators before 0032, then only PNG. AVIF, JPEG 2000, ICNS and RAW still need libraries that aren't built |
| 0038 | No upstream marks: AboutSystem names "The Haiku operating system" in body text, not in the wordmark; the trademark assertion becomes a statement that the name and logos are Haiku's and this build claims no rights or association; the "HAIKU logo" artwork is no longer shipped | This build is not entitled to the Haiku name or logos |
| 0039 | Follow-up to 0029: SIGBUS from an external abort whose FAR the ISS marks invalid (FnV) carries address 0 instead of FAR; the `EXCP_ILL_STATE` comment corrected (PSTATE.IL at EL0 comes from a legal return whose SPSR had IL set) | From a review of 0029; `trap-test.sh` still passes 14/14 |
| 0040 | All 19 codec libraries built in prose-packages go into the image: libpng16, libjpeg_turbo, tiff, libwebp, giflib, openjpeg, lcms; libogg, libvorbis, flac, opus, speex, speexdsp, mpg123, lame, wavpack; libtheora, libvpx, dav1d (`UserBuildConfig`). The build resolves libiconv and libtool_libltdl from the same local repository. `repositories/HaikuPorts/arm64` keeps every local package in one sorted block | The image had only the five libraries the translators link against. Needs the packages in `generated/download/` (`private_workspace/addpkg.sh`). `packages/boot-test.sh packages/tests/codecs.sh codec_check` passes 19/19 on the image's own packages |
| 0041 | openssl3, bc and wget, which the minimum profile asks for, plus what they need: ca_root_certificates (2026_07_16-1 replaces upstream's entry), zstd, gettext_libintl; all from prose-packages | The build printed "AddHaikuImagePackages: package openssl3/bc/wget not available!" and went on without them; Haiku's server has none of them for arm64. `packages/tests/minimum.sh` checks them on the target |
| 0042 | The `prose-*` build profiles (`prose-mmc` is what `scripts/build-image.sh` builds): the regular image -- its applications, preferences and demos, minus what needs Mesa -- with `PROSE_VM_GUEST` set, so the regular definition leaves out the sound-card, Bluetooth and Wi-Fi drivers (one hangs the kernel's driver probing under QEMU; none can work in a VM), plus the packages prosepkg builds: OpenSSL, xz, zip, unzip, gzip, tar, which, Vision, Pe and ~60 applications from HaikuArchives and haikuports, listed in `DefaultBuildProfiles`; fluidlite listed so `libmidi.so` (needs the build feature) and MidiPlayer are in; the CJK font; the regular definition adds only the translators 0037 did not move into minimum; the generic package info no longer requires x86 wifi firmware on arm64; host name `prose` | The minimum image had no user software at all. `packages/tests/apps.sh` checks the result on the target; `private_workspace/skip-first-boot-prompt.sh` seeds the `Locale settings` file, because a regular image otherwise waits in FirstBootPrompt |
| 0043 | 34 more applications in the `prose-*` profile: A-Book, BookmarkConverter, Brie, BSAP, DebugMonitor, EmailViews, FairTrade, FlyingTroll, FolderShaper, Fortuna, Globe, Havoc, HDialog, KeyCursor, Microbe, Moe, MrPeeps, NightAndDay, OpenOriginPackage, PhotoGrabber, PonyExpress, PPViewer, RemoteControl, RomanNumbers, SuperPrefs, USBDeskbar, VWget, XRS, ConvertToLF, LockWorkstation, WaveView, CherryBlossom, BeTeX, PecoBeat (+ InternalMIDI, taglib, libexif, libsndfile, libsamplerate) | The second prosepkg batch; the image has 123 applications, 11 demos, 23 preferences |
| 0044 | NetSurf 3.11 (Be API front end, JavaScript via duktape) in the `prose-*` profile, with its libraries: libcss, libdom, hubbub, libparserutils, libwapcaplet, libnsbmp, libnsgif, libnslog, libnspsl, libnsutils, libsvgtiny, libutf8proc | The image had no web browser. Built by prosepkg; its `nsgenbind` generator is built for the Mac (`PROSE_HOST_TOOLS`). `packages/tests/netsurf.sh` starts it on the target |
| 0045 | libedit listed, so the `libedit` build feature is on and Debugger is built and shipped | debug_server needs Debugger to write a crash report ("Failed to start debugger: No such file or directory" when NetSurf crashed on exit) |
| 0046 | `timgmsoundfont` in the `prose-*` profile and in the arm64 HaikuPorts list | Haiku's own softsynth (BSynth) loads `data/synth/TimGM6mb.sf2`; the package does not exist for arm64 upstream, so the image had no soundfont and MidiPlayer was silent. Data only, built by prosepkg |
| 0047 | Three demo tunes in `data/music`, packaged for the image | MidiPlayer opened to an empty file picker: nothing in the image was a `.mid`. Generated by `tools/music/tunes.py`; Beethoven and Pachelbel, long out of copyright |
| 0048 | MidiPlayer: an Output menu (the built-in synthesizer, the Mac through the Prose MIDI device, any MIDI device or application; live input follows it), its own song player (a Standard MIDI File parser and a thread sending through a Midi Kit 2 producer: seek, pause, loop, tempo, per-channel volume, channel state chased on seek and output changes), a 16-channel view with instruments and note activity, position/time, File ▸ Open, `--output <name>` on the command line; the volume setting is read back (it went into the window position) | It could only play through the built-in synthesizer and crashed (double free in `~BLooper`) when its window closed with a song loaded: `BMidi::Stop()` forgets the player thread's id, so `~BMidi` never waits for it, and the thread wakes into the freed `BMidiStore` and calls the song hook with the destroyed window. It no longer uses `BMidiSynthFile`. `packages/tests/midiplayer.sh` |
| 0049 | `HAIKU_IMAGE_LABEL` and `HAIKU_MMC_LABEL` set to `Prose` in the `prose-*` profile | The disk Tracker shows on the desktop, and its window title, said "Haiku"; both default to that name |
| 0050 | Midi Kit (libmidi): the destructors wait for the run thread (`BMidi` remembers it, `_StopRunThread()`; the song hook is cleared first; a hook that deletes its own player, or a thread stuck over 5 s, is not waited for and no longer written to after `Run()`), and the input glues (`BMidiGlue`, `BMidiPortGlue`) deliver under a lock, with `Detach()`, called by `~BMidiSynth` before it may delete `be_synth` (kept if a delivery does not end), `~BMidiStore`, `~BMidiPort` and `~BMidi`; MidiPlayer's `SynthBridge` gets the same | Any program deleting a `BMidiSynthFile` while it played could crash: the store's run thread went on in the freed store and called the song hook (the old MidiPlayer's double free at quit), and the synthesizer input's thread, asleep until a note was due, played it into the deleted `be_synth` (SEGV in `fluid_synth_noteon`). Made on `prose` before 0049, so its commit there has no `Prose-Patch` record (`apply-patches.sh` finds it present). `packages/tests/midikit.sh` |
| 0051 | `BMidi::Stop()` stops the run thread before it sends "all notes off"; `BMidiSynthFile::LoadFile()` loads the soundfont (`LoadAllInstruments()`) instead of the unimplemented per-instrument `LoadInstrument()` | `Stop()` and deleting a playing `BMidiSynthFile` took 10 s: the song went on between the 16 "all notes off" messages, a note each, and the notes after the last one kept sounding. The soundfont was loaded at the first note, from the input's thread, so a song was held up for that long (8 s under QEMU) and then played what was due all at once. Now 1 s at most, one note of the song. `packages/tests/midikit.sh` |
| 0052 | `HAIKU_MMC_LABEL` passed through to the FAT initializer in `MMCImage` | The variable was declared and never read: both actions hardcoded `'Haiku Boot'`, so the EFI System Partition kept that label whatever the profile set |
| 0053 | `build/jam/DeskbarCategories`: nine folders for the Deskbar's Applications menu (Accessories, Development, Files, Games, Graphics, Internet, Multimedia, Office, System) and the folder of each entry, Haiku's own and the ports'; the haiku package puts its links there (`DeskbarCategory` rule), and prosepkg moves the ports' entries when `scripts/local-packages.sh` copies their packages in. An unlisted entry stays in Applications | The menu was one column of 106 applications. The Deskbar merges the menu from every package by folder name, so a package's flat entry cannot be hidden from outside it: the entries have to be in the folders in the packages. The applications stay in `/boot/system/apps`. `packages/tests/deskbar.sh` |
| 0054 | `prose_portal`, a service that answers the host: PING, RUN (a command line, returning its exit status with stdout and stderr apart) and INFO (uptime, memory, whether Tracker and the Deskbar are running). Framed protocol in `headers/private/prose/prose_portal.h`, carried today by TCP on the machine's own network | The host could drive the machine but learn nothing from it: a screenshot has no exit status, and every host-side guess at "booted" measured drawing settling -- 18 s to over 50 s on identical fresh boots -- when the desktop is up in about four. `/boot/home/config/settings/prose/no-automation` stops it listening |

From 0009 on, patches are `git format-patch` output of single commits on the `vz-fork` branch (they may touch several files) and apply with `git apply` or `git am`. 0031–0038 sit between 0023 and 0024 on the branch and were exported later. They apply after 0030, and the whole series reproduces `vz-fork` exactly (0001–0038 checked file by file on a clean hrev60122). 0031 and 0035 carry PNG files as git binary patches, which `patch(1)` cannot apply. 0033, 0037 and 0040 need the local packages (`private_workspace/README.md`, "Local packages"). `../prose-branding-01-artwork.patch` is the earlier branding for the main tree (it also sets `uname` to "Kronkite"); it applies to a plain hrev60122 but not on top of this series.

Building applies them: `scripts/build-image.sh` runs `scripts/apply-patches.sh`,
which puts `/Volumes/HaikuSrc/haiku` on the branch `prose` (upstream's
hrev60122 + these patches, one commit each, `Prose-Patch: <file> <sha256>`
recorded in every commit) and applies whatever is missing; then
`scripts/local-packages.sh`, which copies the packages 0033, 0037 and 0040
list (built by `scripts/prosepkg`; the package server has none of them) into
`generated/download/` and turns downloads off. See BUILDING.md §5 and §7.

The `vz-fork` branch of the same repository, where 0001–0040 were made, has
the same tree as `prose` (`94f281d`); its checkout under
`/Volumes/HaikuSrc/private_workspace/` was removed on 2026-09-19.

To add a change: commit it on `prose`, then

```sh
/Volumes/xb/HaikuArmQemu/scripts/export-patch.sh     # writes the next NNNN-<subject>.patch
```

It first puts the record into the commit (`Prose-Patch: <file> <hash>`, the
hash being of the diff alone, `scripts/patch-diff-hash.sh`, which is the same
for the commit and the file); then add the row above. A patch whose diff was
edited after it was applied makes `apply-patches.sh` stop and say so (the hash
no longer matches the record); run `export-patch.sh` again on the amended
commit, or rebuild `prose` from the patches (`git branch -D prose`, build).
0001–0008 are plain `git diff` output and each cover one file.
