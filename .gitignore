# Case-sensitive build volume image + mountpoint symlink
HaikuBuild.sparsebundle*
/src  # the HaikuSrc symlink at repo root only

# Local scratch
*.log
.DS_Store

# Sprint 0 tool builds and test runs
build/
work/
__pycache__/

# Prose packages: sources and build trees live on the case-sensitive volume
packages/src
