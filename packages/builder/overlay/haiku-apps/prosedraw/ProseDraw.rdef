/* The application's identity, added to the executable by the recipe: the
 * Makefile builds it without resources. */
resource app_signature "application/x-vnd.prose.ProseDraw";

resource app_flags B_MULTIPLE_LAUNCH;

resource app_version {
	major  = 0,
	middle = 1,
	minor  = 0,
	variety = B_APPV_ALPHA,
	internal = 0,
	short_info = "ProseDraw",
	long_info  = "ProseDraw 0.1 - a drawing program for Prose"
};
